diff --git a/node_modules/@felixgeelhaar/govee-api-client/dist/infrastructure/GoveeDeviceRepository.js b/node_modules/@felixgeelhaar/govee-api-client/dist/infrastructure/GoveeDeviceRepository.js
index 1a4e853..dc5552c 100644
--- a/node_modules/@felixgeelhaar/govee-api-client/dist/infrastructure/GoveeDeviceRepository.js
+++ b/node_modules/@felixgeelhaar/govee-api-client/dist/infrastructure/GoveeDeviceRepository.js
@@ -276,7 +276,11 @@ export class GoveeDeviceRepository {
         }
         else if (cmdObj.name === 'color') {
             instance = 'colorRgb';
-            value = cmdObj.value;
+            // Convert {r, g, b} object to packed integer for Govee API v2
+            const cv = cmdObj.value;
+            value = (typeof cv === 'object' && cv !== null && 'r' in cv)
+                ? (cv.r * 65536 + cv.g * 256 + cv.b)
+                : cv;
         }
         else if (cmdObj.name === 'colorTem') {
             instance = 'colorTemperatureK';
